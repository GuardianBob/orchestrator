orchestrator-review.md
# orchestrator state (reviews, gates, notifications, resume snapshot)
.orchestrator/

# node / vitest
node_modules/
coverage/
.vitest-cache/
*.log

# Windows reserved name — created accidentally by tools emitting `> NUL`
NUL
